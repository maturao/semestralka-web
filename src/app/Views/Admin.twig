{% extends "Base.twig" %}

{% import "Macros.twig" as macros %}

{% block content %}

    <div class="row">
        <div class="col-md-3">
            <h3>Administrace</h3>
            <ul class="nav nav-pills flex-column">
                <li class="nav-item">
                    <a class="nav-link" href="{{ macros.link("User", "adminUsers") }}">Správa uživatelů</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ macros.link("Article", "adminArticles") }}">Správa článků</a>
                </li>
            </ul>
            <hr class="d-sm-none">
        </div>

        <div class="col-md-9">
            <h1>
                {{ title }}
            </h1>
            {% block administration %}

            {% endblock %}
        </div>
    </div>

{% endblock %}